import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.basketball.manager',
  appName: 'Basketball Manager',
  webDir: 'dist'
};

export default config;
